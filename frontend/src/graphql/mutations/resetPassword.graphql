mutation ResetPassword($options: ResetPasswordInput!) {
  resetPassword(options: $options) {
    error {
      message
    }
    errors {
      ...FieldError
    }
    user {
      ...User
    }
  }
}
